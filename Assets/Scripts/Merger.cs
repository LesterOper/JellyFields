using UnityEngine;

namespace DefaultNamespace
{
    public class Merger
    {
        public static MapTile[,] Merge(MapTile[,] tiles, Vector2 poitionInMap)
        {
            Debug.Log("MERGE");
            return tiles;
        }
    }
}